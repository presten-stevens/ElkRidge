---
phase: 15-multi-tenant-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/manager/types/instance.ts
  - src/manager/config/instances.ts
  - src/manager/services/pm2-manager.ts
  - src/manager/services/instance-health.ts
  - src/manager/services/__tests__/pm2-manager.test.ts
  - src/manager/services/__tests__/instance-health.test.ts
  - config/instances.json
autonomous: true
requirements: [EOPS-04]

must_haves:
  truths:
    - "Instance config loaded and validated from instances.json with Zod"
    - "PM2 programmatic API wrapped in Promise-based async methods"
    - "Health aggregation queries all worker instances in parallel"
    - "env:VAR_NAME references in config are resolved from process.env"
  artifacts:
    - path: "src/manager/types/instance.ts"
      provides: "InstanceConfig, InstanceStatus, AggregatedHealth type definitions"
      exports: ["InstanceConfig", "InstanceStatus", "AggregatedHealth", "InstanceHealth", "instanceSchema", "instancesConfigSchema"]
    - path: "src/manager/config/instances.ts"
      provides: "Load, validate, and resolve instances.json"
      exports: ["loadInstances", "resolveEnvRefs"]
    - path: "src/manager/services/pm2-manager.ts"
      provides: "Promise-wrapped PM2 lifecycle operations"
      exports: ["connectPM2", "disconnectPM2", "startInstance", "stopInstance", "restartInstance", "deleteInstance", "listProcesses", "describeProcess"]
    - path: "src/manager/services/instance-health.ts"
      provides: "Aggregated health check across all worker instances"
      exports: ["getAggregatedHealth"]
    - path: "config/instances.json"
      provides: "Instance registry matching current single-instance setup"
  key_links:
    - from: "src/manager/config/instances.ts"
      to: "config/instances.json"
      via: "fs.readFile + JSON.parse + Zod validate"
      pattern: "readFile.*instances\\.json"
    - from: "src/manager/services/pm2-manager.ts"
      to: "pm2"
      via: "pm2 programmatic API (connect, start, stop, list, describe)"
      pattern: "import pm2"
    - from: "src/manager/services/instance-health.ts"
      to: "worker /health endpoints"
      via: "fetch with Promise.allSettled and 3s timeout"
      pattern: "Promise\\.allSettled"
---

<objective>
Build the foundation services for the multi-tenant management layer: type definitions, instance config loader, PM2 lifecycle wrapper, and health aggregation service.

Purpose: These are the core building blocks that the management API routes (Plan 02) will consume. Every management operation depends on loading config, talking to PM2, or checking instance health.
Output: Type contracts, config loader, PM2 manager service, health aggregation service, instance registry JSON, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-tenant-architecture/15-RESEARCH.md

<interfaces>
<!-- Existing project patterns the executor needs -->

From src/config/env.ts (worker env validation pattern):
```typescript
export const envSchema = z.object({
  PORT: z.string().default('3000').transform(Number),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
  BLUEBUBBLES_URL: z.string().url(),
  BLUEBUBBLES_PASSWORD: z.string().min(1),
  CRM_WEBHOOK_URL: z.string().url().optional(),
  ALERT_WEBHOOK_URL: z.string().url().optional(),
  API_KEY: z.string().min(16).optional(),
  LOG_LEVEL: z.enum(['fatal', 'error', 'warn', 'info', 'debug', 'trace']).default('info'),
  // ... transforms for numeric strings
});
export const env = parsed.data;
export type Env = z.infer<typeof envSchema>;
```

From src/types/health.ts (worker health response):
```typescript
export interface HealthResponse {
  status: 'healthy' | 'degraded' | 'down';
  bluebubbles: { status: string; version: string };
  imessage: { authenticated: boolean };
  system: { macosVersion: string };
  timestamp: string;
  lastChecked: string | null;
}
```

From ecosystem.config.js (current PM2 config):
```javascript
export default {
  apps: [{
    name: 'bb-tyler-iphone',
    script: 'dist/server.js',
    env_file: '.env.tyler_iphone',
    instances: 1,
    autorestart: true,
    max_restarts: 10,
    restart_delay: 1000,
    max_memory_restart: '256M',
  }],
};
```

From package.json: type: "module", verbatimModuleSyntax in tsconfig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define type contracts and instance config loader</name>
  <files>src/manager/types/instance.ts, src/manager/config/instances.ts, config/instances.json</files>
  <read_first>
    - src/config/env.ts (Zod validation pattern, env schema structure)
    - src/types/health.ts (HealthResponse interface for health aggregation types)
    - ecosystem.config.js (current single-instance PM2 config to mirror in instances.json)
    - package.json (confirm "type": "module", check if pm2 needs installing)
  </read_first>
  <action>
    1. Run `npm install pm2` to add PM2 as a project dependency (it is currently only a global CLI tool).

    2. Create `src/manager/types/instance.ts` with ALL type definitions for the management layer:

       - `instanceSchema` (Zod): object with fields `id` (regex /^[a-z0-9-]+$/), `phoneNumber` (starts with +), `port` (int 1024-65535), `bluebubbles` object with `url` (string URL) and `password` (string min 1), `apiKey` (string min 1), `crmWebhookUrl` (optional URL string), `alertWebhookUrl` (optional URL string), `enabled` (boolean default true).
       - `instancesConfigSchema` (Zod): object with `instances` array of instanceSchema, plus `.superRefine()` that checks for duplicate ports and duplicate IDs -- add a ZodIssueCode.custom issue for each duplicate found.
       - `InstanceConfig` type: inferred from instanceSchema
       - `InstancesConfig` type: inferred from instancesConfigSchema
       - `InstanceStatus` interface: `{ id: string; phoneNumber: string; port: number; enabled: boolean; pm2Status: string; pid: number | null; uptime: number | null; memory: number | null; restarts: number | null }`
       - `InstanceHealth` interface: `{ status: 'healthy' | 'degraded' | 'down'; bluebubbles: { status: string; version: string }; imessage: { authenticated: boolean }; port: number; pm2Status: string; uptime: number | null; error?: string }`
       - `AggregatedHealth` interface: `{ status: 'healthy' | 'degraded' | 'down'; instances: Record<string, InstanceHealth>; summary: { total: number; healthy: number; degraded: number; down: number }; timestamp: string }`

       Use `import { z } from 'zod';`. Export all schemas, types, and interfaces as named exports.

    3. Create `src/manager/config/instances.ts`:

       - `resolveEnvRefs(vars: Record<string, string>): Record<string, string>` -- for each value starting with `env:`, look up `process.env[value.slice(4)]`, throw Error if missing with message `Missing env var: {envKey} (referenced by {key})`. Return resolved record.
       - `loadInstances(configPath?: string): InstancesConfig` -- reads `config/instances.json` (or provided path) synchronously with `fs.readFileSync`, parses JSON, validates with `instancesConfigSchema.parse()`. Resolve env: references in each instance's `bluebubbles.password` and `apiKey` fields by calling resolveEnvRefs on those two fields (mutate the parsed result before returning). Default configPath should use `path.resolve(process.cwd(), 'config/instances.json')`.

       Use `import fs from 'node:fs';` and `import path from 'node:path';`.

    4. Create `config/instances.json` that mirrors the current single-instance setup:
       ```json
       {
         "instances": [
           {
             "id": "tyler-iphone",
             "phoneNumber": "+18015551234",
             "port": 3000,
             "bluebubbles": {
               "url": "http://localhost:1234",
               "password": "env:BB_PASSWORD_TYLER_IPHONE"
             },
             "apiKey": "env:API_KEY_TYLER_IPHONE",
             "crmWebhookUrl": "https://crm.example.com/webhook/iphone",
             "alertWebhookUrl": "https://alerts.example.com/iphone",
             "enabled": true
           }
         ]
       }
       ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/manager/types/instance.ts exports instanceSchema, instancesConfigSchema, InstanceConfig, InstancesConfig, InstanceStatus, InstanceHealth, AggregatedHealth
    - src/manager/config/instances.ts exports loadInstances and resolveEnvRefs
    - config/instances.json exists with one instance entry and env: references for secrets
    - pm2 appears in package.json dependencies
    - npx tsc --noEmit passes (no type errors)
  </acceptance_criteria>
  <done>Type contracts defined and exported, config loader reads and validates instances.json with env: reference resolution, pm2 installed as dependency</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PM2 manager service and instance health aggregation with tests</name>
  <files>src/manager/services/pm2-manager.ts, src/manager/services/instance-health.ts, src/manager/services/__tests__/pm2-manager.test.ts, src/manager/services/__tests__/instance-health.test.ts</files>
  <read_first>
    - src/manager/types/instance.ts (type contracts from Task 1)
    - src/manager/config/instances.ts (config loader from Task 1)
    - src/services/health-monitor.ts (existing health monitor pattern -- singleton, init/shutdown lifecycle)
    - src/services/bluebubbles.ts (existing singleton factory pattern)
    - .planning/phases/15-multi-tenant-architecture/15-RESEARCH.md (PM2 programmatic API patterns, aggregated health pattern)
  </read_first>
  <behavior>
    PM2 Manager:
    - connectPM2() resolves when pm2.connect succeeds, rejects on error
    - disconnectPM2() calls pm2.disconnect
    - startInstance(config) calls pm2.start with correct name (bb-{id}), script (dist/server.js), env vars resolved from instance config, autorestart:true, max_restarts:10, restart_delay:1000
    - stopInstance(id) calls pm2.stop with name bb-{id}
    - restartInstance(id) calls pm2.restart with name bb-{id}
    - deleteInstance(id) calls pm2.delete with name bb-{id}
    - listProcesses() returns pm2.list result
    - describeProcess(id) calls pm2.describe with name bb-{id}
    - getInstanceStatus(config) merges config data with PM2 process description (pid, uptime, memory, restarts, pm2Status)

    Instance Health:
    - getAggregatedHealth with all healthy instances returns status "healthy"
    - getAggregatedHealth with one down instance returns status "degraded"
    - getAggregatedHealth with all down instances returns status "down"
    - Individual instance health check uses 3s timeout via AbortSignal.timeout
    - Uses Promise.allSettled for parallel checks (rejected = down)
  </behavior>
  <action>
    1. Create `src/manager/services/pm2-manager.ts`:

       - Import pm2 with `import pm2 from 'pm2';` (CJS interop -- pm2 is a CJS module, so with verbatimModuleSyntax this may need `import pm2Module from 'pm2'; const pm2 = pm2Module;` -- test and adjust).
       - Import `InstanceConfig`, `InstanceStatus` from `../types/instance.js`.
       - Import `resolveEnvRefs` from `../config/instances.js`.

       Implement Promise-wrapped PM2 functions:
       - `connectPM2(): Promise<void>` -- `pm2.connect(err => err ? reject(err) : resolve())`
       - `disconnectPM2(): void` -- `pm2.disconnect()`
       - `startInstance(instance: InstanceConfig): Promise<void>` -- build env object: `{ PORT: String(instance.port), NODE_ENV: 'production', BLUEBUBBLES_URL: instance.bluebubbles.url, BLUEBUBBLES_PASSWORD: instance.bluebubbles.password, API_KEY: instance.apiKey, CRM_WEBHOOK_URL: instance.crmWebhookUrl ?? '', ALERT_WEBHOOK_URL: instance.alertWebhookUrl ?? '', LOG_LEVEL: 'info' }`. Call `pm2.start({ name: 'bb-' + instance.id, script: 'dist/server.js', env, instances: 1, autorestart: true, max_restarts: 10, restart_delay: 1000, max_memory_restart: '256M' }, callback)`.
       - `stopInstance(id: string): Promise<void>` -- `pm2.stop('bb-' + id, callback)`
       - `restartInstance(id: string): Promise<void>` -- `pm2.restart('bb-' + id, callback)`
       - `deleteInstance(id: string): Promise<void>` -- `pm2.delete('bb-' + id, callback)`
       - `listProcesses(): Promise<pm2.ProcessDescription[]>` -- `pm2.list(callback)`
       - `describeProcess(id: string): Promise<pm2.ProcessDescription[]>` -- `pm2.describe('bb-' + id, callback)`
       - `getInstanceStatus(instance: InstanceConfig): Promise<InstanceStatus>` -- calls describeProcess, extracts pm2_env.status, pid, pm_uptime, monit.memory, unstable_restarts from first element. Returns InstanceStatus with fallbacks (null for missing fields, 'not_found' for pm2Status if no results).

    2. Create `src/manager/services/instance-health.ts`:

       - Import `InstanceConfig`, `InstanceHealth`, `AggregatedHealth` from `../types/instance.js`.

       Implement `getAggregatedHealth(instances: InstanceConfig[]): Promise<AggregatedHealth>`:
       - Use `Promise.allSettled()` to fetch `http://127.0.0.1:${inst.port}/health` for each instance with `AbortSignal.timeout(3000)`.
       - For fulfilled: parse JSON as HealthResponse, map to InstanceHealth (add port, pm2Status placeholder 'online', uptime: null).
       - For rejected: return `{ status: 'down', bluebubbles: { status: 'unreachable', version: '' }, imessage: { authenticated: false }, port: inst.port, pm2Status: 'unknown', uptime: null, error: 'unreachable' }`.
       - Count healthy, degraded, down. Overall status: all down = 'down', any degraded or some down = 'degraded', all healthy = 'healthy'.
       - Return `{ status, instances: Record<string, InstanceHealth>, summary: { total, healthy, degraded, down }, timestamp: new Date().toISOString() }`.

    3. Create test files with mocked PM2 and fetch:

       `src/manager/services/__tests__/pm2-manager.test.ts`:
       - Mock `pm2` module entirely via `vi.mock('pm2', ...)`. Provide mock implementations for connect, disconnect, start, stop, restart, delete, list, describe.
       - Test connectPM2 resolves on success and rejects on error.
       - Test startInstance calls pm2.start with name 'bb-{id}' and correct env vars.
       - Test stopInstance calls pm2.stop with 'bb-{id}'.
       - Test getInstanceStatus returns correct InstanceStatus shape with fallbacks when process not found.

       `src/manager/services/__tests__/instance-health.test.ts`:
       - Mock global `fetch` via `vi.stubGlobal('fetch', ...)`.
       - Test all healthy: two instances both return 200 with status 'healthy' -> overall 'healthy', summary counts correct.
       - Test partial down: one healthy, one fetch rejects -> overall 'degraded'.
       - Test all down: all fetch reject -> overall 'down'.
       - Test timeout: verify AbortSignal.timeout(3000) is passed.
  </action>
  <verify>
    <automated>npx vitest run src/manager/services/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/manager/services/pm2-manager.ts exports connectPM2, disconnectPM2, startInstance, stopInstance, restartInstance, deleteInstance, listProcesses, describeProcess, getInstanceStatus
    - src/manager/services/instance-health.ts exports getAggregatedHealth
    - pm2-manager.test.ts has tests for connect, start, stop, getInstanceStatus (all pass with mocked pm2)
    - instance-health.test.ts has tests for all-healthy, partial-down, all-down scenarios (all pass with mocked fetch)
    - npx vitest run src/manager/services/__tests__/ passes all tests
  </acceptance_criteria>
  <done>PM2 manager wraps all lifecycle operations in Promise API, health aggregation queries workers in parallel with 3s timeout, all tests pass with mocked dependencies</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors across new files)
- `npx vitest run src/manager/ --reporter=verbose` passes all tests
- `config/instances.json` validates against the Zod schema
- pm2 is in package.json dependencies
</verification>

<success_criteria>
All management layer foundation services are implemented and tested:
- Type contracts define the shapes for instance config, status, and health
- Config loader reads, validates, and resolves instances.json
- PM2 manager provides async lifecycle operations
- Health aggregation queries workers in parallel with timeout handling
- Unit tests verify PM2 and health logic with mocked dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-tenant-architecture/15-01-SUMMARY.md`
</output>
