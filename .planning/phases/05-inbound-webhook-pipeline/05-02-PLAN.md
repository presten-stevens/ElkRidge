---
phase: 05-inbound-webhook-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package.json
  - src/services/bb-events.ts
  - src/services/__tests__/bb-events.test.ts
  - src/server.ts
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-06
  - SEND-04

must_haves:
  truths:
    - "When BB emits a new-message event with isFromMe=false, a webhook fires to CRM_WEBHOOK_URL with sender, body, timestamp, threadId"
    - "When BB emits a new-message event with isFromMe=true, no webhook fires (outbound filter)"
    - "When BB emits an updated-message event with isFromMe=true, a delivery_confirmation webhook fires"
    - "Duplicate events (same GUID within 60s) are deduped before relay"
    - "last_synced_at is updated after each successfully processed message"
    - "Socket.IO connects to BB on server startup with password auth"
    - "Socket.IO auto-reconnects on disconnect with exponential backoff"
  artifacts:
    - path: "src/services/bb-events.ts"
      provides: "Socket.IO event listener, event processing pipeline"
      exports: ["initBBEvents", "shutdownBBEvents"]
    - path: "src/server.ts"
      provides: "Initializes BB events after app.listen()"
      contains: "initBBEvents"
    - path: "package.json"
      provides: "socket.io-client dependency"
      contains: "socket.io-client"
  key_links:
    - from: "src/services/bb-events.ts"
      to: "src/services/dedup.ts"
      via: "import DedupBuffer"
      pattern: "import.*DedupBuffer.*dedup"
    - from: "src/services/bb-events.ts"
      to: "src/services/webhook-relay.ts"
      via: "import relayToCRM, mapInboundMessage, mapDeliveryConfirmation"
      pattern: "import.*relayToCRM.*webhook-relay"
    - from: "src/services/bb-events.ts"
      to: "src/services/sync-state.ts"
      via: "import writeSyncState"
      pattern: "import.*writeSyncState.*sync-state"
    - from: "src/server.ts"
      to: "src/services/bb-events.ts"
      via: "import initBBEvents"
      pattern: "import.*initBBEvents.*bb-events"
---

<objective>
Wire the Socket.IO event listener that connects to BlueBubbles, processes new-message and updated-message events through the dedup/relay/sync pipeline, and initialize it from server.ts on startup.

Purpose: This is the core event pipeline -- it connects all Plan 01 services into the live message processing flow that fulfills HOOK-01 (inbound webhook fires) and SEND-04 (delivery confirmations forwarded).
Output: Working inbound webhook pipeline that receives BB events and relays to CRM.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-inbound-webhook-pipeline/05-CONTEXT.md
@.planning/phases/05-inbound-webhook-pipeline/05-RESEARCH.md
@.planning/phases/05-inbound-webhook-pipeline/05-01-SUMMARY.md

@src/server.ts
@src/config/env.ts
@src/middleware/logger.ts
@src/services/dedup.ts
@src/services/sync-state.ts
@src/services/webhook-relay.ts
@src/types/bluebubbles.ts
@src/types/webhook.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/services/dedup.ts:
```typescript
export class DedupBuffer {
  constructor(ttlMs?: number);
  isDuplicate(guid: string): boolean;
  destroy(): void;
}
```

From src/services/sync-state.ts:
```typescript
export function readSyncState(): Promise<string | null>;
export function writeSyncState(lastSyncedAt: string): Promise<void>;
```

From src/services/webhook-relay.ts:
```typescript
export function mapInboundMessage(data: BBSocketMessage): InboundMessagePayload;
export function mapDeliveryConfirmation(data: BBSocketMessage): DeliveryConfirmationPayload;
export function relayToCRM(payload: WebhookPayload): Promise<void>;
```

From src/types/bluebubbles.ts:
```typescript
export interface BBSocketMessage {
  guid: string; text: string | null; isFromMe: boolean;
  dateCreated: number; dateDelivered: number; dateRead: number;
  handle: { address: string } | null;
  chats: Array<{ guid: string }>;
  attachments: Array<{ guid: string; mimeType: string; transferName: string; totalBytes: number }>;
  associatedMessageGuid: string; associatedMessageType: string; error: number;
}
```

From src/types/webhook.ts:
```typescript
export type WebhookPayload = InboundMessagePayload | DeliveryConfirmationPayload;
```

From src/config/env.ts:
```typescript
BLUEBUBBLES_URL: z.string().url(),
BLUEBUBBLES_PASSWORD: z.string().min(1),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install socket.io-client dependency</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
    Run `npm install socket.io-client` to add socket.io-client as a production dependency. Version 4.8.x per research recommendation.
  </action>
  <verify>
    <automated>node -e "require('socket.io-client'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "socket.io-client" package.json
  </acceptance_criteria>
  <done>socket.io-client is in package.json dependencies and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BB events service and server.ts wiring</name>
  <files>
    src/services/bb-events.ts,
    src/services/__tests__/bb-events.test.ts,
    src/server.ts
  </files>
  <read_first>
    src/server.ts,
    src/config/env.ts,
    src/middleware/logger.ts,
    src/services/dedup.ts,
    src/services/sync-state.ts,
    src/services/webhook-relay.ts,
    src/types/bluebubbles.ts,
    src/types/webhook.ts
  </read_first>
  <behavior>
    BB Events (src/services/__tests__/bb-events.test.ts):
    - Mock socket.io-client to return a mock socket with on/connect/disconnect methods
    - initBBEvents() creates Socket.IO connection to BLUEBUBBLES_URL with auth: { password: BLUEBUBBLES_PASSWORD }
    - initBBEvents() registers listeners for "new-message" and "updated-message" events
    - On "new-message" with isFromMe=false: calls dedup check, if not duplicate calls mapInboundMessage + relayToCRM + writeSyncState
    - On "new-message" with isFromMe=true: skips processing (outbound filter per Pitfall 5)
    - On "new-message" with duplicate GUID: skips relay (dedup working)
    - On "updated-message" with isFromMe=true: calls mapDeliveryConfirmation + relayToCRM (delivery confirmation for our sent messages)
    - On "updated-message" with isFromMe=false: skips (not our message, no delivery tracking needed)
    - shutdownBBEvents() disconnects socket and destroys dedup buffer
    - writeSyncState called with ISO timestamp after successful processing
  </behavior>
  <action>
    1. **Create `src/services/bb-events.ts`** (per D-01, D-02, D-03, D-15, D-16):

       ```typescript
       import { io, type Socket } from 'socket.io-client';
       ```

       Module-level variables: `let socket: Socket | null = null;` and `let dedup: DedupBuffer | null = null;`

       **initBBEvents(): void**
       - Create `dedup = new DedupBuffer()` (60s TTL default)
       - Create socket connection (per D-01, research Pattern 1):
         ```
         socket = io(env.BLUEBUBBLES_URL, {
           auth: { password: env.BLUEBUBBLES_PASSWORD },
           reconnection: true,
           reconnectionDelay: 1000,
           reconnectionDelayMax: 30000,
           reconnectionAttempts: Infinity,
         });
         ```
       - Register event listeners:
         - `socket.on('connect', () => logger.info('Connected to BlueBubbles WebSocket'))`
         - `socket.on('disconnect', (reason) => logger.warn({ reason }, 'Disconnected from BlueBubbles WebSocket'))`
         - `socket.on('connect_error', () => logger.error('BlueBubbles WebSocket connection error'))` — do NOT log error object (per Pitfall 4, SECR-04)
         - `socket.on('new-message', (data: BBSocketMessage) => handleNewMessage(data))`
         - `socket.on('updated-message', (data: BBSocketMessage) => handleUpdatedMessage(data))`

       **async handleNewMessage(data: BBSocketMessage): Promise<void>**
       - If `data.isFromMe` is true, return early (per Pitfall 5 — outbound messages filtered)
       - If `dedup.isDuplicate(data.guid)`, return early (per D-04)
       - Call `const payload = mapInboundMessage(data)` (per D-08)
       - Call `await relayToCRM(payload)` (per D-07)
       - Call `await writeSyncState(new Date(data.dateCreated).toISOString())` (per D-12)
       - Wrap in try/catch, log errors but do NOT crash the event loop

       **async handleUpdatedMessage(data: BBSocketMessage): Promise<void>**
       - If `data.isFromMe` is false, return early (only track delivery of OUR messages per SEND-04)
       - If `dedup.isDuplicate(data.guid)`, return early
       - Call `const payload = mapDeliveryConfirmation(data)` (per D-09)
       - Call `await relayToCRM(payload)` (per D-07)
       - Wrap in try/catch, log errors but do NOT crash

       **shutdownBBEvents(): void**
       - If socket: `socket.disconnect()`, set to null
       - If dedup: `dedup.destroy()`, set to null

       Export: `initBBEvents`, `shutdownBBEvents`

    2. **Update `src/server.ts`** (per D-16):
       - Add import: `import { initBBEvents } from './services/bb-events.js';`
       - Inside the `app.listen()` callback, AFTER the logger.info line, add: `initBBEvents();`

    3. **Tests**: Mock `socket.io-client` module entirely with `vi.mock('socket.io-client')`. Create a mock socket object with `.on()` that stores handlers, then trigger handlers manually in tests. Mock `../services/dedup.js`, `../services/webhook-relay.js`, `../services/sync-state.js` to verify call patterns. Use `vi.mock('../config/env.js')` for env values.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/bb-events.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "initBBEvents" src/services/bb-events.ts
    - grep -q "shutdownBBEvents" src/services/bb-events.ts
    - grep -q "new-message" src/services/bb-events.ts
    - grep -q "updated-message" src/services/bb-events.ts
    - grep -q "isFromMe" src/services/bb-events.ts
    - grep -q "initBBEvents" src/server.ts
    - grep -q "socket.io-client" src/services/bb-events.ts
    - npx vitest run src/services/__tests__/bb-events.test.ts exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>
    BB events service connects to BlueBubbles via Socket.IO on startup. new-message events with isFromMe=false are deduped, mapped to InboundMessagePayload, relayed to CRM, and sync state updated. updated-message events with isFromMe=true are mapped to DeliveryConfirmationPayload and relayed. Outbound messages and duplicates are filtered. server.ts calls initBBEvents() after listen. Full test suite green.
  </done>
</task>

</tasks>

<verification>
Full pipeline verification:
- `npx vitest run src/services/__tests__/bb-events.test.ts` — event handling, dedup, relay, sync
- `npm test` — full suite green (no regressions from server.ts change)
- `grep -r "initBBEvents" src/server.ts` — wired into startup
- `grep -r "socket.io-client" package.json` — dependency installed
</verification>

<success_criteria>
- Socket.IO connects to BB with password auth on server startup
- new-message events with isFromMe=false trigger inbound webhook to CRM (HOOK-01, HOOK-02)
- Duplicate events are filtered by dedup buffer (HOOK-04 verified end-to-end)
- updated-message events with isFromMe=true trigger delivery confirmation webhook (SEND-04)
- last_synced_at updated after each processed message (HOOK-06 verified end-to-end)
- Outbound messages (isFromMe=true on new-message) are filtered (Pitfall 5)
- Auto-reconnection configured with exponential backoff (D-02)
- All tests green, full suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-inbound-webhook-pipeline/05-02-SUMMARY.md`
</output>
