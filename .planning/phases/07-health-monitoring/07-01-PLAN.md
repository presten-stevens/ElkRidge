---
phase: 07-health-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/health.ts
  - src/services/health.ts
  - src/services/__tests__/health.test.ts
  - src/routes/health.ts
  - src/routes/__tests__/health.test.ts
  - src/routes/index.ts
  - src/config/env.ts
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
must_haves:
  truths:
    - "GET /health returns live status of BlueBubbles service, iPhone connection, and iMessage auth state"
    - "GET /health response includes macOS version and BlueBubbles version"
    - "GET /health returns 'healthy' when BB is reachable and iMessage is authenticated"
    - "GET /health returns 'degraded' when BB is reachable but iMessage is not authenticated"
    - "GET /health returns 'down' when BB is unreachable"
  artifacts:
    - path: "src/types/health.ts"
      provides: "HealthResponse, BBServerInfo, AlertPayload type definitions"
      exports: ["HealthResponse", "BBServerInfo", "AlertPayload"]
    - path: "src/services/health.ts"
      provides: "checkHealth function that queries BB server info"
      exports: ["checkHealth"]
    - path: "src/routes/health.ts"
      provides: "GET /health route handler"
      exports: ["healthRouter"]
    - path: "src/routes/index.ts"
      provides: "Health route mounted in main router"
      contains: "healthRouter"
  key_links:
    - from: "src/routes/health.ts"
      to: "src/services/health.ts"
      via: "import checkHealth"
      pattern: "checkHealth"
    - from: "src/services/health.ts"
      to: "BlueBubblesClient"
      via: "client.request('/api/v1/server/info')"
      pattern: "request.*server/info"
    - from: "src/routes/index.ts"
      to: "src/routes/health.ts"
      via: "router.use(healthRouter)"
      pattern: "healthRouter"
---

<objective>
Health check endpoint with real BB status reporting.

Purpose: Tyler can hit GET /health to see live status of BlueBubbles, iMessage auth, macOS version, and BB version -- detecting silent failures like iMessage sign-outs that a shallow 200 check would miss.

Output: Types, health check service, GET /health route with tests, env schema extended with health polling config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-health-monitoring/07-CONTEXT.md
@.planning/phases/07-health-monitoring/07-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From src/services/bluebubbles.ts:
```typescript
export class BlueBubblesClient {
  async request<T>(path: string, options?: RequestInit): Promise<T>;
}
export function getBBClient(): BlueBubblesClient;
```

From src/config/env.ts:
```typescript
export const envSchema = z.object({
  // ... existing fields
  ALERT_WEBHOOK_URL: z.string().url().optional(),
});
export const env: Env;
```

From src/routes/index.ts:
```typescript
import { Router } from 'express';
export const router = Router();
// Currently mounts sendRouter and conversationsRouter
```

From src/types/error-codes.ts:
```typescript
export const ERROR_CODES = {
  BB_OFFLINE: 'BB_OFFLINE',
  // ... others
} as const;
```

From src/app.ts:
```typescript
// Health route must be mounted BEFORE any future auth middleware (D-04)
// Currently: helmet -> json -> httpLogger -> router -> errorHandler
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health types, check service, and env schema extension</name>
  <files>src/types/health.ts, src/services/health.ts, src/services/__tests__/health.test.ts, src/config/env.ts</files>
  <read_first>
    - src/services/bluebubbles.ts (request method signature, getBBClient pattern)
    - src/config/env.ts (env schema structure, existing ALERT_WEBHOOK_URL)
    - src/services/dedup.ts (.unref pattern reference)
    - src/types/error-codes.ts (ERROR_CODES.BB_OFFLINE)
    - .planning/phases/07-health-monitoring/07-RESEARCH.md (BB server info shape, health status mapping)
  </read_first>
  <behavior>
    - checkHealth returns status "healthy" when BB responds with detected_imessage set
    - checkHealth returns status "degraded" when BB responds with detected_imessage null
    - checkHealth returns status "degraded" when BB responds with detected_imessage empty string
    - checkHealth returns status "down" when BB client.request throws (BB_OFFLINE)
    - Response includes bluebubbles.version from server_version field
    - Response includes system.macosVersion from os_version field
    - Response includes imessage.authenticated boolean derived from detected_imessage
    - Response includes ISO timestamp
    - Response includes lastChecked field (null when not set)
  </behavior>
  <action>
    1. Create src/types/health.ts with these types (per D-01, D-09):
       - BBServerInfo: { os_version: string, server_version: string, private_api: boolean, helper_connected: boolean, detected_imessage: string | null } (fields from BB /api/v1/server/info)
       - HealthResponse: { status: 'healthy' | 'degraded' | 'down', bluebubbles: { status: string, version: string }, imessage: { authenticated: boolean }, system: { macosVersion: string }, timestamp: string, lastChecked: string | null }
       - AlertPayload: { type: 'downtime_alert', service: 'bluebubbles' | 'imessage', status: string, message: string, timestamp: string }

    2. Create src/services/__tests__/health.test.ts FIRST (TDD red):
       - Mock BlueBubblesClient.request to return canned BBServerInfo
       - Test healthy: detected_imessage = "user@icloud.com" -> status "healthy", authenticated true
       - Test degraded (null): detected_imessage = null -> status "degraded", authenticated false
       - Test degraded (empty): detected_imessage = "" -> status "degraded", authenticated false
       - Test down: client.request throws AppError(BB_OFFLINE) -> status "down", version "", macosVersion ""
       - Test version fields: os_version "15.3.1" -> macosVersion "15.3.1", server_version "1.9.9" -> version "1.9.9"
       - Run tests -- they MUST fail (red)

    3. Create src/services/health.ts (TDD green):
       - Export async function checkHealth(client: BlueBubblesClient): Promise<HealthResponse>
       - Query client.request<BBServerInfo>('/api/v1/server/info')
       - Map response per D-02: authenticated = detected_imessage !== null && detected_imessage !== ''
       - status = authenticated ? 'healthy' : 'degraded'
       - On catch: return { status: 'down', bluebubbles: { status: 'unreachable', version: '' }, imessage: { authenticated: false }, system: { macosVersion: '' }, timestamp: ISO string, lastChecked: null }
       - Run tests -- they MUST pass (green)

    4. Extend src/config/env.ts envSchema -- add two fields:
       - HEALTH_POLL_INTERVAL_MS: z.string().default('60000').transform(Number) (per D-05)
       - ALERT_AFTER_FAILURES: z.string().default('2').transform(Number) (per D-08)
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/health.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "BBServerInfo" src/types/health.ts
    - grep -q "HealthResponse" src/types/health.ts
    - grep -q "AlertPayload" src/types/health.ts
    - grep -q "export.*checkHealth" src/services/health.ts
    - grep -q "server/info" src/services/health.ts
    - grep -q "HEALTH_POLL_INTERVAL_MS" src/config/env.ts
    - grep -q "ALERT_AFTER_FAILURES" src/config/env.ts
    - grep -q "healthy.*degraded.*down" src/services/__tests__/health.test.ts
  </acceptance_criteria>
  <done>checkHealth returns correct healthy/degraded/down status based on BB server info. Env schema extended with HEALTH_POLL_INTERVAL_MS and ALERT_AFTER_FAILURES. All unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Health route, router mount, and integration test</name>
  <files>src/routes/health.ts, src/routes/__tests__/health.test.ts, src/routes/index.ts</files>
  <read_first>
    - src/routes/index.ts (current router structure)
    - src/routes/send.ts (thin route pattern to follow)
    - src/routes/conversations.ts (route pattern reference)
    - src/app.ts (middleware order -- health route position for D-04)
    - src/services/health.ts (just created in Task 1)
    - src/types/health.ts (just created in Task 1)
  </read_first>
  <behavior>
    - GET /health returns 200 with HealthResponse JSON
    - GET /health returns status "down" when BB is unreachable (still 200 -- it is reporting status, not failing)
    - Response Content-Type is application/json
    - Response body matches D-01 shape exactly
  </behavior>
  <action>
    1. Create src/routes/__tests__/health.test.ts FIRST (TDD red):
       - Use supertest against createApp()
       - Mock getBBClient to return a mock client
       - Mock checkHealth to return canned HealthResponse
       - Test: GET /health returns 200 with correct JSON shape
       - Test: GET /health when BB is down still returns 200 (status field = "down")
       - Run tests -- they MUST fail (red)

    2. Create src/routes/health.ts (TDD green):
       - Import Router from express, checkHealth from services/health, getBBClient from services/bluebubbles
       - Export const healthRouter = Router()
       - GET /health handler: calls checkHealth(getBBClient()), returns res.json(result)
       - No auth required (per D-04) -- this is a status endpoint
       - Run tests -- they MUST pass (green)

    3. Mount in src/routes/index.ts:
       - Import healthRouter from './health.js'
       - Add router.use(healthRouter) BEFORE sendRouter and conversationsRouter (so Phase 8 auth middleware can skip it per D-04)

    4. Run full test suite to confirm no regressions: npx vitest run --reporter=verbose
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "healthRouter" src/routes/health.ts
    - grep -q "GET.*health" src/routes/health.ts || grep -q "get.*health" src/routes/health.ts
    - grep -q "checkHealth" src/routes/health.ts
    - grep -q "healthRouter" src/routes/index.ts
    - grep -q "supertest\|createApp" src/routes/__tests__/health.test.ts
  </acceptance_criteria>
  <done>GET /health endpoint returns live BB status. Health route mounted first in router (before other routes). Integration test confirms 200 response with correct shape. Full test suite green.</done>
</task>

</tasks>

<verification>
- npx vitest run --reporter=verbose (all tests pass)
- GET /health returns JSON matching D-01 shape
- Health check queries BB /api/v1/server/info (not a shallow 200)
- Env schema includes HEALTH_POLL_INTERVAL_MS and ALERT_AFTER_FAILURES
</verification>

<success_criteria>
GET /health returns real-time status (healthy/degraded/down) based on live BB server info query, including macOS version, BB version, and iMessage auth state. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-health-monitoring/07-01-SUMMARY.md`
</output>
